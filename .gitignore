/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json

# Per-server deployment settings (domain, DB password). Copied from
# deploy/config.example.sh on the server — never committed.
/deploy/config.sh
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Scribe API docs are regenerated by `php artisan scribe:generate` (see
# docs/AUTH_MODULE.md) — not hand-edited, so not tracked.
/.scribe
/public/vendor/scribe
/resources/views/scribe

# Partner API reference. Regenerated by `php artisan partner:docs` from the
# code it documents — every file under it is output, never a source. The bundle
# itself lands in storage/app/private/partner-api-docs, which storage's own
# .gitignore already excludes.
/.scribe_partner
